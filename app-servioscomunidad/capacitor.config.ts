import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.servioscomunidad',
  appName: 'app-servioscomunidad',
  webDir: 'www'
};

export default config;
